package de.xiekang.UUID_Tutorial;

import java.util.UUID;

public class UUID_Tutorial {

    public static void main(String[] args) {
        //IDEA: UUID uuid = UUID.nameUUIDFromBytes(some_bytes);
        //IDEA: UUID uuid = UUID.fromString(uuidHexDigitString);
        System.out.println(UUID.randomUUID());
    }
}
